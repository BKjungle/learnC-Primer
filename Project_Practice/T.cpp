#include "T.h"

// ¿ªÊ¼

int main()
{
	std::cout << "Hello World ~" << std::endl;
	float weight = static_cast<float>(128.0) / static_cast<float>(256.0);
	std::cout << "weight:" << weight << "--"<< int(abs(0.8)) << std::endl;
	show();
	return 0;
}